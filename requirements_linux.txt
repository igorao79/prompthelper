# Кроссплатформенные зависимости для Linux/Windows/macOS
# Генератор лендингов v2.0

# === ОСНОВНЫЕ БИБЛИОТЕКИ ===
requests>=2.31.0
beautifulsoup4>=4.12.2
lxml>=4.9.3
fake-useragent>=1.4.0
certifi>=2023.7.22
urllib3>=2.0.4

# === ОБРАБОТКА ИЗОБРАЖЕНИЙ ===
Pillow>=10.0.0
numpy>=1.24.0

# === GUI (встроен в Python) ===
# tkinter - встроен в Python, дополнительная установка не нужна

# === АВТОМАТИЗАЦИЯ (опционально) ===
# pyautogui>=0.9.54  # Закомментировано - может быть проблематично в Linux без X11
# pygetwindow>=0.0.9  # Только для Windows

# === КРОССПЛАТФОРМЕННЫЕ УТИЛИТЫ ===
pathlib2>=2.3.7;python_version<"3.4"
chardet>=5.2.0
pyyaml>=6.0

# === AI ОБРАБОТКА ИЗОБРАЖЕНИЙ (опционально) ===
# Эти библиотеки большие, устанавливать только если нужно AI удаление фона
# rembg>=2.0.50
# onnxruntime>=1.16.0
# opencv-python>=4.8.0
# scikit-image>=0.21.0

# === СБОРКА EXE (только для Windows/macOS) ===
# pyinstaller>=6.0.0

# === LINUX СПЕЦИФИЧНЫЕ ПАКЕТЫ ===
# Для установки в Linux рекомендуется:
# sudo apt-get install python3-tk python3-pil python3-pil.imagetk
# sudo apt-get install fonts-noto-color-emoji fonts-dejavu fonts-liberation

# === ДОПОЛНИТЕЛЬНЫЕ ШРИФТЫ ДЛЯ LINUX ===
# Устанавливаются через систему:
# Ubuntu/Debian: sudo apt-get install fonts-noto-color-emoji
# Fedora: sudo dnf install google-noto-emoji-color-fonts
# Arch: sudo pacman -S noto-fonts-emoji

# === УСТАНОВКА CURSOR AI В LINUX ===
# 1. AppImage: скачать с сайта cursor.so
# 2. Snap: sudo snap install cursor
# 3. Flatpak: flatpak install flathub com.cursor.Cursor
# 4. Deb пакет: скачать .deb и установить через dpkg

# === МИНИМАЛЬНАЯ УСТАНОВКА ===
# Для базовой работы достаточно:
# pip install requests beautifulsoup4 lxml Pillow

# === ПОЛНАЯ УСТАНОВКА ===
# pip install -r requirements_linux.txt

# === ПРИМЕЧАНИЯ ===
# - tkinter обычно встроен в Python
# - Шрифты лучше устанавливать через системный менеджер пакетов
# - pyautogui может требовать дополнительных настроек в Linux
# - AI функции опциональны и потребляют много ресурсов 